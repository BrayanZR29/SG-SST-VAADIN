# Configuracion SG-SST (Proyecto Java Puro)
app.nombre=Sistema de Gestion SST
app.version=1.0.0

# Base de datos local
db.url=jdbc:postgresql://localhost:5432/sst
db.usuario=postgres
db.contrasena=0619
db.pool.maxSize=10
db.pool.minIdle=2

# Servidor web embebido
server.port=8080